version: '3.3'

services:
  movies_db:
    image: mysql:latest
    volumes:
      - ./_infra/init_inventory.sql:/docker-entrypoint-initdb.d/init.sql:ro
    restart: always
    ports:
      - 3306:3306
    environment:
      MYSQL_DATABASE: inventory
      MYSQL_ALLOW_EMPTY_PASSWORD: "true"
      MYSQL_USER: me
      MYSQL_PASSWORD: 123
